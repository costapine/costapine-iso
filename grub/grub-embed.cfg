# Costapine Linux - Early GRUB Configuration
# Embedded in the standalone EFI binary
# Searches for the ISO filesystem and loads the real grub.cfg

search --no-floppy --label COSTAPINE --set=root
set prefix=($root)/boot/grub
configfile $prefix/grub.cfg
